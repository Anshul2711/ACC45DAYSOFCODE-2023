#include <iostream>
using namespace std;

int main() {
    int T,N;
    cin>>T;
    while(T>0)
    {
        cin>>N;
        if(N%2==0)
        {
        cout<<N-2<<" "<<"1"<<" "<<"1"<<endl;
        }else{
        cout<<1<<" "<<(N-1)/2<<" "<<(N-1)/2<<endl;
        }
        T--;
    }
	return 0;
}
