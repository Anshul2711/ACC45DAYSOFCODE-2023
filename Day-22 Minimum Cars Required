#include <iostream>

using namespace std;

int main() {
    int T,N;
    cin >> T;
    while(T>0)
    {
        cin >> N;
        int result = N / 4;
        if (N % 4 != 0) 
        {
            result++;
        }
        cout << result << endl;
        T--;
    }
    return 0;
}
