#include <iostream>
using namespace std;

int main() {
    int T,X;
    cin>>T;
    while(T>0)
    {
        int X;
        cin>>X;
        if(X>15)
        {
            cout<<"NO"<<endl;
        }
        else
        {
                cout<<"YES"<<endl;
        }
        T--;
    }
	return 0;
}
